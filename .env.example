# Target for the contract tests. Defaults to the public hosted Conduit.
# To point at a local or different implementation of the same specification,
# copy this file to .env and change one line.
CONDUIT_API_URL=https://api.realworld.show/api
