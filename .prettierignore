# The specification and the chain artifacts are wrapped by hand.
# spec/conduit-api.md is a verbatim copy of an external document; reformatting it
# would break the property that makes it a copy.
# pipeline/*.md are reviewed as diffs — reflowing lines would add noise with no content.
*.md
package-lock.json
node_modules/
playwright-report/
test-results/
